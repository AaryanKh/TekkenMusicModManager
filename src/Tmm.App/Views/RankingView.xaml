<UserControl x:Class="Tmm.App.Views.RankingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Tmm.App.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance Type=vm:RankingViewModel}">
    <DockPanel Margin="16">
        <!-- Header -->
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
            <TextBlock Style="{StaticResource H1}">
                <Run Text="Pick a slot for "/><Run Text="{Binding SongTitle, Mode=OneWay}" FontWeight="Bold"/>
            </TextBlock>
            <TextBlock Style="{StaticResource Muted}" Margin="0,4,0,0">
                <Run Text="{Binding TotalFits, Mode=OneWay}"/><Run Text=" of the game's jukebox slots can take a loop from this song inside the stretch cap. Compatibility is a geometric mean of loop fit, seam quality and intro fit — one bad component sinks the slot on purpose."/>
            </TextBlock>
            <Border Background="#33E5B84C" BorderBrush="{StaticResource WarnBrush}" BorderThickness="1" CornerRadius="4" Padding="10,6" Margin="0,8,0,0"
                    Visibility="{Binding Provisional, Converter={StaticResource Vis}}">
                <TextBlock Text="{Binding ProvisionalNote}" Style="{StaticResource Warning}"/>
            </Border>
        </StackPanel>

        <!-- Filters -->
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Padding="12,8" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <WrapPanel VerticalAlignment="Center">
                    <TextBox Width="200" Margin="0,0,10,0" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" ToolTip="Filter by title"/>
                    <ComboBox Width="150" Margin="0,0,10,0" ItemsSource="{Binding Games}" SelectedItem="{Binding GameFilter}"/>
                    <ComboBox Width="120" Margin="0,0,10,0" ItemsSource="{Binding Bands}" SelectedItem="{Binding LengthBand}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,14,0">
                        <TextBlock Text="Min %" Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <Slider Width="90" Minimum="0" Maximum="100" Value="{Binding MinPercent}" TickFrequency="10" IsSnapToTickEnabled="True"/>
                        <TextBlock Text="{Binding MinPercent, StringFormat={}{0:0}}" Width="26" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </StackPanel>
                    <CheckBox Content="Loop-only slots" IsChecked="{Binding LoopOnly}"/>
                    <CheckBox Content="Hide slots in use" IsChecked="{Binding HideClaimed}"/>
                    <Button Content="Reset" Style="{StaticResource SmallButton}" Command="{Binding ResetFiltersCommand}" VerticalAlignment="Center"/>
                </WrapPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0"
                            ToolTip="How much time-stretch is acceptable. Re-ranks instantly: loop candidates are slot-independent.">
                    <TextBlock Text="Stretch cap" Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <Slider Width="140" Minimum="0.5" Maximum="15" Value="{Binding StretchCapPercent}" TickFrequency="0.5" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding StretchCapPercent, StringFormat={}±{0:0.0}%}" Width="52" VerticalAlignment="Center" Margin="6,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border DockPanel.Dock="Bottom" Style="{StaticResource Card}" Padding="12,8" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Selected.Title, FallbackValue='Select a row to see why it scored the way it did.'}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Selected.Explain}" Style="{StaticResource Muted}"/>
                </StackPanel>
                <Button Grid.Column="1" Content="Use this slot  ›" Style="{StaticResource AccentButton}" Command="{Binding ChooseCommand}" Margin="12,0,0,0"/>
            </Grid>
        </Border>

        <!-- Table -->
        <DataGrid ItemsSource="{Binding Rows}" SelectedItem="{Binding Selected}" MouseDoubleClick="OnRowDoubleClick" PreviewKeyDown="OnKeyDown"
                  EnableRowVirtualization="True" VirtualizingPanel.IsVirtualizing="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Rank}" Width="44"/>
                <DataGridTemplateColumn Header="Match" Width="90" SortMemberPath="Headline">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Border Width="8" Height="8" CornerRadius="4" Margin="0,0,6,0" Background="{Binding Headline, Converter={StaticResource ScoreBrush}}"/>
                                <TextBlock Text="{Binding Headline, StringFormat={}{0:0}%}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Track" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Game" Binding="{Binding Game}" Width="120"/>
                <DataGridTextColumn Header="Length" Binding="{Binding Length}" Width="110"/>
                <DataGridTextColumn Header="Bars" Binding="{Binding Bars}" Width="50"/>
                <DataGridTextColumn Header="Stretch" Binding="{Binding StretchPercent, StringFormat={}{0:0.0}%}" Width="66"/>
                <DataGridTextColumn Header="Loop fit" Binding="{Binding LoopFit, StringFormat={}{0:0}}" Width="66"/>
                <DataGridTextColumn Header="Seam" Binding="{Binding Seam, StringFormat={}{0:0}}" Width="56"/>
                <DataGridTextColumn Header="Intro" Binding="{Binding IntroFit, StringFormat={}{0:0}}" Width="56"/>
                <DataGridTextColumn Header="Used" Binding="{Binding Coverage, StringFormat={}{0:0}%}" Width="56"/>
                <DataGridTextColumn Header="" Binding="{Binding ClaimedLabel}" Width="56"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
