Put the community UnrealPak build here (UnrealPak.exe plus the DLLs that ship with it,
the one from the usual Tekken 8 modding tutorial).
Then in TekkenMusicModManager open Settings and set the UnrealPak path to:
  <this folder>\UnrealPak.exe
UnrealPak is not bundled for licensing reasons.
