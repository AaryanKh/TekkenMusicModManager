Put ffmpeg.exe here (e.g. from https://www.gyan.dev/ffmpeg/builds/ or `winget install Gyan.FFmpeg`).
Then in TekkenMusicModManager open Settings and set the ffmpeg path to:
  <this folder>\ffmpeg.exe
ffmpeg is not bundled for licensing reasons.
